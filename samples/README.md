# Samples

Put short example clips here (e.g. `sample1.wav`) and their transcriptions, so the
README can show edge-model output. Audio files are gitignored by default; commit a few
small ones explicitly if you want them in the repo, or host them and link instead.
